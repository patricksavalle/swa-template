# API

Backend placeholder.

Put serverless functions, HTTP handlers, or backend endpoints here. Keep runtime
choice explicit: Azure Functions, Node, Python, .NET, Go, or another stack.

Expected responsibilities:

- External input validation
- Authentication and authorization boundaries
- Calls to external services
- API contracts consumed by `app/`
