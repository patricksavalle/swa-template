# App

Frontend placeholder.

Put the static web app, SPA, or frontend package here. Keep framework-specific
files out of the template until a concrete project chooses a runtime.

Expected responsibilities:

- User interface
- Client-side routing
- Static assets
- Calls to the API boundary
