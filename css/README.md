# CSS

Tailwind CSS input, compiled stylesheet output, and static styling assets.

Edit `css/tailwind.css` for shared Tailwind imports and theme declarations.
`css/site.css` is generated by `npm run build:css` and copied into `dist/`.
