{
  "name": "swa-template",
  "version": "0.1.0",
  "private": true,
  "description": "Generic Static Web App project template with CI/CD and infrastructure documentation placeholders.",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "validate": "node scripts/validate-template.mjs",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "clean": "node scripts/clean-dist.mjs",
    "build": "npm run clean && eleventy --config=eleventy.config.js && tsc --project tsconfig.json",
    "dev": "eleventy --config=eleventy.config.js --serve",
    "test": "node scripts/test-placeholder.mjs",
    "ci": "npm run validate && npm run lint && npm run typecheck && npm run build && npm test"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.1.6",
    "@eslint/js": "^10.0.1",
    "eslint": "^10.4.1",
    "eslint-plugin-boundaries": "^6.0.2",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.61.0"
  }
}
