# Packages

Shared package placeholder.

Use this directory only for code that has at least two real consumers, such as
shared schemas, generated clients, domain types, or pure utility modules.

Avoid speculative shared libraries.
