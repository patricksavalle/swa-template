# Business Logic Tier

Pure application and domain decisions.

Prefer deterministic functions that receive values and return values. Keep I/O,
framework state, browser APIs, and network calls outside this tier.
