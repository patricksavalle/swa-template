# Infrastructure Tier

Adapters for external systems and platform APIs.

Examples:

- HTTP clients
- storage adapters
- authentication adapters
- browser API wrappers
- deployment/runtime configuration readers

Keep domain decisions out of this tier.
