# User Interface Tier

Screens, components, routes, and interaction flows.

This tier may depend on business logic contracts and may call infrastructure
through explicit adapters. Keep rendering concerns out of `businesslogic/`.
